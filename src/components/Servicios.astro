---
const services = [
  {
    title: "Mantenimiento Preventivo",
    description: "Realizamos mantenimiento preventivo para asegurar que tu autobús esté siempre en perfecto estado.",
    image: "/assets/preventivo.png"
  },
  {
    title: "Reparación de Motores",
    description: "Reparamos motores de autobuses con piezas originales para garantizar una larga vida útil.",
    image: "/assets/motor.jpg"
  },
  {
    title: "Cambio de Aceite y Filtros",
    description: "Cambiar el aceite y los filtros de tu autobús es esencial para un rendimiento óptimo del motor.",
    image: "/assets/aceite.jpg"
  }
];
---

<div class="container mx-auto px-4 py-8 pt-24">
  <!-- Título -->
  <h2 class="text-3xl font-semibold text-center m-8">Servicios</h2>

  {services.map((service, index) => (
    <div class={`flex flex-col md:flex-row items-center mb-8 ${index % 2 === 0 ? 'md:flex-row-reverse' : ''}`}>
      <img src={service.image} alt={service.title} class="w-full md:w-1/3 h-auto mb-4 md:mb-0 rounded-lg shadow-lg" />
      <div class="md:w-2/3 md:pl-8">
        <h3 class="text-xl font-bold mb-4">{service.title}</h3>
        <p class="text-lg text-gray-600">{service.description}</p>
      </div>
    </div>
  ))}
</div>
